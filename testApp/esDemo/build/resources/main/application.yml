
spring:
  main:
    allow-bean-definition-overriding: true
  application:
    name: DeepmetaSaver
  data:
    elasticsearch:
      cluster-name: elasticsearch_lamhirh
      cluster-nodes: localhost:9200

config:
  saveIndexName: myindex